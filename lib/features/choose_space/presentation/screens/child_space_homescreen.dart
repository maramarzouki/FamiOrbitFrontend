import 'package:flutter/material.dart';

class ChildSpaceHomescreen extends StatelessWidget {
  const ChildSpaceHomescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}